import { PageFirstDirective } from './page-first.directive';

describe('PageFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new PageFirstDirective();
    expect(directive).toBeTruthy();
  });
});
