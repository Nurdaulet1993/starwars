import { PageLastDirective } from './page-last.directive';

describe('PageLastDirective', () => {
  it('should create an instance', () => {
    const directive = new PageLastDirective();
    expect(directive).toBeTruthy();
  });
});
