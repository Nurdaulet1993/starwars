import { PagePrevDirective } from './page-prev.directive';

describe('PagePrevDirective', () => {
  it('should create an instance', () => {
    const directive = new PagePrevDirective();
    expect(directive).toBeTruthy();
  });
});
