import { PageNextDirective } from './page-next.directive';

describe('PageNextDirective', () => {
  it('should create an instance', () => {
    const directive = new PageNextDirective();
    expect(directive).toBeTruthy();
  });
});
