import { CharacterLayoutDirective } from './character-layout.directive';

describe('CharacterLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
